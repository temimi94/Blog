<!doctype html>
<html lang="fr">
<head>
    <meta charset="utf-8">
    <meta name="viewport" content="width=device-width, initial-scale=1, shrink-to-fit=no">
    <meta name="description" content="Blog">
    <meta name="author" content="Hamdaoui rogaya">
    <script src="https://use.fontawesome.com/releases/v6.3.0/js/all.js" crossorigin="anonymous"></script>
    <title>Blog</title>

    <link href="../public/template/css/styles.css" rel="stylesheet">
</head>
<body>
 <nav class="navbar navbar-expand-lg navbar-light" id="mainNav">
            <div class="container px-4 px-lg-5">
                <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarResponsive" aria-controls="navbarResponsive" aria-expanded="false" aria-label="Toggle navigation">
                    Menu
                    <i class="fas fa-bars"></i>
                </button>
                <div class="collapse navbar-collapse" id="navbarResponsive">
                     {{  include ('layout/navbar.twig') }}
                </div>
            </div>
        </nav>
    <div class="page">
     <header class="masthead" style="background-image: url('../public/img/background.jpg'); padding-top: 16rem;">
            <div class="container position-relative px-4 px-lg-5">
                <div class="row gx-4 gx-lg-5 justify-content-center">
                    <div class="col-md-10 col-lg-8 col-xl-7">
                        <div class="site-heading">
                        <h3>Hamdaoui Rogaya</h3>
                            <span class="subheading">Développeuse Web</span>

                        </div>
                    </div>
                </div>
                 {% block title %}{% endblock %}
            </div>
        </header>
                <div class="container pb-5">
                    {% if erreur is defined %}
                        <div class="alert alert-danger text-center" role="alert">{{ erreur }}</div>
                    {% endif %}
                    {% if success is defined %}
                        <div class="alert alert-success text-center" role="alert">{{ success }}</div>
                    {% endif %}
                    {% block content %}{% endblock %}
                </div>

        </main>

        <footer class="site-footer footer bg-dark text-white-50">
            <div class="container mt-3 mb-3 text-md-center">
              {{  include('layout/footer.twig') }}
            </div>
        </footer>
    </div>
    <script src="https://code.jquery.com/jquery-3.2.1.slim.min.js"></script>
    <script src="https://maxcdn.bootstrapcdn.com/bootstrap/4.0.0/js/bootstrap.min.js"></script>
</body>
</html>

